# Data (the connectome is large -- don't commit it)
data/connectome/*.csv
data/**/*.parquet
data/**/*.feather
data/male_cns_raw/          # raw male-CNS feather files (hundreds of MB)
# NOTE: data/connectome/male_cns_subgraph.npz IS committed (small, the real fly)

# Training outputs
runs/
*.npz
*.pt
# ...but DO commit the real male-CNS subgraph (small; it's the real fly)
!data/connectome/male_cns_subgraph.npz

# Visualiser intermediate (the standalone viz/fly_viz.html embeds this)
viz/fly_bundle.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.vscode/
.idea/

# Generated web build (served copies live at index.html and docs/index.html)
viz/fly_viz.html
